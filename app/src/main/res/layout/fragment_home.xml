<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">
    <!-- TODO: Update blank fragment layout -->

    <LinearLayout
        android:id="@+id/linear_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:orientation="horizontal">

        <android.support.design.widget.TabLayout
            android:id="@+id/tab_layout"
            android:layout_width="0dp"
            android:layout_height="35dp"
            android:layout_weight="1"
            android:minWidth="20dp"
            app:tabGravity="fill"
            app:tabIndicatorColor="@color/colorPrimary"
            app:tabMaxWidth="50dp"
            app:tabMode="scrollable"
            app:tabSelectedTextColor="@color/tab_title_selected"
            app:tabTextColor="@color/tab_title_unselected" />

        <!-- <ImageButton
             android:id="@+id/btn_down_open"
             android:layout_width="45dp"
             android:layout_height="35dp"
             android:src="@mipmap/ic_down_expand"
             android:background="@drawable/btn_expand_bgcolor"/>-->

    </LinearLayout>


    <android.support.v4.view.ViewPager
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/linear_layout"/>

</RelativeLayout>


